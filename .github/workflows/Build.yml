name: Build VPet

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive 

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore NuGet packages
        run: dotnet restore VPet.sln

      - name: Build VPet (Release)
        run: >
          dotnet build VPet.sln --configuration Release --no-restore
          "/flp:logfile=build.log;verbosity=detailed"
          "/flp1:logfile=build.err.log;errorsonly"

      - name: Upload build artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: VPet-Simulator-ReadyToRun
          # ИСПРАВЛЕНО: забираем собранный релиз из VPet.Solution, 
          # где автоматически объединяются Core, Windows, макеты и моды.
          path: VPet.Solution/bin/Release/net8.0-windows/
          if-no-files-found: error

      - name: Upload build log artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            build.log
            build.err.log
          if-no-files-found: warn
